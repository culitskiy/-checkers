import styled from 'styled-components';

const Button = styled.button`
    width: 150px;
    height: 60px;
    margin-top: 50px;

    border-radius: 20px;

    outline: none;
    cursor: pointer;
`;

export default Button;
